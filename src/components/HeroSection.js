import React from "react";
import "../styles/HeroSection.css";

const HeroSection = () => (
  <section className="hero">
    <h1>Empowering Businesses with AI-Powered Solutions</h1>
    <button className="cta-button">Get Started</button>
  </section>
);

export default HeroSection;
